services:
  - type: web
    name: car-project
    runtime: image
    repo: https://github.com/YOUR_USERNAME/your-project
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PORT
        value: 8000
      - key: DB_PATH
        value: /data/database.sqlite